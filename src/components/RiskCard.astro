---
interface Props {
  number: string;
  title: string;
  pullquote: string;
  pullquoteSource: string;
}

const { number, title, pullquote, pullquoteSource } = Astro.props;
---

<div class="flex flex-col chrome-border bg-[var(--color-surface-1)]/50 p-6 md:p-8 lg:p-10">
  <div class="mb-5">
    <span class="text-xs font-mono tracking-widest text-[var(--color-accent)]/40 uppercase">{number}</span>
  </div>
  <div class="flex items-start gap-3 mb-4">
    <div class="shrink-0 mt-1 text-[var(--color-accent)]/60">
      <slot name="icon" />
    </div>
    <h3 class="text-lg md:text-xl font-semibold text-white leading-snug">{title}</h3>
  </div>
  <div class="text-sm text-neutral-400 leading-relaxed space-y-3 mb-6">
    <slot />
  </div>
  <blockquote class="mt-auto pt-5 border-t border-[var(--color-border)]">
    <p class="text-sm italic text-neutral-300 leading-relaxed">{pullquote}</p>
    <cite class="block mt-2 text-xs not-italic text-neutral-500">{pullquoteSource}</cite>
  </blockquote>
</div>
